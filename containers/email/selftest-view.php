<div class="w3-card w3-padding">
    <h4>Self-Test</h4>
    <p>Run the CLI self-test to verify configuration, template rendering, and log driver output.</p>
    <pre>php containers/email/selftest.php</pre>
    <p>The self-test will perform a non-destructive SMTP handshake (when configured), render templates, and write a log entry using the log driver.</p>
</div>
